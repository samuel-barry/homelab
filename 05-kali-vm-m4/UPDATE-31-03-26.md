# Update: 31/03/26

## VMware Fusion (M4 Silicon)
- Bypassed macOS Sequoia Gatekeeper using xattr.
- Manually initialized hypervisor services via terminal to fix GUI hang.

## Kali Linux VM
- Deployed ARM64 architecture on M4.
- Fixed "No root file system" error by switching to Guided Partitioning.
- VM is fully operational with Xfce desktop.

## Tailscale & SSH
- Identified SSH password rejection over 100.x.x.x IP.
- Enabled Tailscale SSH for identity-based access.
- Successfully connected from Mac to Pi without a password.
